# Block everything by default — only .enc files may be added
*

# Allow repo infrastructure (already tracked, never added fresh by students)
!.gitignore
!.devcontainer/
!.devcontainer/**
!Makefile

# The only data files allowed are encrypted Verilog
!*.enc
